# The only parameter that changes in the execution of the program is the RUN_TYPE variable. 
# If we want to run the program with the training file then we assign "TEST",
#  otherwise we assign "TRAINING".
